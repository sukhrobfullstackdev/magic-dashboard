import { ManageTeamView } from '@components/views/manage-team-view/manage-team-view';
import { Metadata } from 'next';

export default ManageTeamView;
export const metadata: Metadata = { title: 'Account - Manage Team' };
