import { PricingView } from '@components/views/pricing-view/pricing-view';
import { Metadata } from 'next';

export default PricingView;
export const metadata: Metadata = { title: 'Magic | Pricing' };
