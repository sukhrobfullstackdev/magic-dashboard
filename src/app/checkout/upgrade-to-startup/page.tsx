import { UpgradeToStartupView } from '@components/views/upgrade-to-startup-view';
import { Metadata } from 'next';

export default UpgradeToStartupView;
export const metadata: Metadata = { title: 'Magic | Upgrade to Startup' };
