import { UpgradeToGrowthView } from '@components/views/upgrade-to-growth-view';
import { Metadata } from 'next';

export default UpgradeToGrowthView;
export const metadata: Metadata = { title: 'Magic | Upgrade to Growth' };
