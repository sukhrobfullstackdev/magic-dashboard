import { ExternalPreview } from '@components/views/email-customization-view';
import { Metadata } from 'next';

export default ExternalPreview;
export const metadata: Metadata = { title: 'Email - Preview' };
