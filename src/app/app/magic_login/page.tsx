import { MagicLoginView } from '@components/views/magic-login-view/magic-login-view';
import { Metadata } from 'next';

export default MagicLoginView;
export const metadata: Metadata = { title: 'Dashboard - Magic Logins' };
