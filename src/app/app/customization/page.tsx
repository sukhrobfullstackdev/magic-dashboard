import { CustomizationView } from '@components/views/customization-view';
import { Metadata } from 'next';

export default CustomizationView;
export const metadata: Metadata = { title: 'Dashboard - Customization' };
