import { AllAppsView } from '@components/views/all-apps-view/all-apps-view';
import { Metadata } from 'next';

export default AllAppsView;
export const metadata: Metadata = { title: 'Dashboard - All Apps' };
