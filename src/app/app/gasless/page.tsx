import { GasSubsidyView } from '@components/views/gas-relayer-view';
import { Metadata } from 'next';

export default GasSubsidyView;
export const metadata: Metadata = { title: 'Dashboard - Gas Subsidy' };
