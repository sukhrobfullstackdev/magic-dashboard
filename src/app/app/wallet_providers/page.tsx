import { WalletProvidersView } from '@components/views/wallet-providers-view/wallet-providers-view';
import { Metadata } from 'next';

export default WalletProvidersView;
export const metadata: Metadata = { title: 'Dashboard - Wallet Providers' };
