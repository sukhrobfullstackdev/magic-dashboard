import { EmailCustomizationDashboardView } from '@components/views/email-customization-view';
import { Metadata } from 'next';

export default EmailCustomizationDashboardView;
export const metadata: Metadata = { title: 'Dashboard - EmailCustomization' };
