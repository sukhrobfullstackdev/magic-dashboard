import { EmailTemplateView } from '@components/views/email-customization-view';
import { Metadata } from 'next';

export default EmailTemplateView;
export const metadata: Metadata = { title: 'Dashboard - Email' };
